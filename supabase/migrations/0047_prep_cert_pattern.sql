-- 申請準備チェックリスト: 合格証の組み合わせ（申請内容で必要な合格証が変わる）。
--  専門級         … 専門級の合格証あり（同じ分野で就職）→ 専門級のみ
--  別分野・専門級 … 専門級以外の分野で就職（専門級あり）→ 専門外＋専門級
--  専門外・日本語 … 専門級の合格証なし → 専門外＋日本語（日本語は必須）
--  技能評価調書   … 専門級なし・技能実習2号を良好修了 → 技能評価調書
-- '' は未選択（従来どおり合格証3種を表示）
alter table application_prep_checklists
  add column if not exists cert_pattern text not null default ''
  check (cert_pattern in ('', '専門級', '別分野・専門級', '専門外・日本語', '技能評価調書'));
